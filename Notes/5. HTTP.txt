Модуль HTTP
Альтернативний підхід до створення серверних програм --
використання власних модулів/пакетів/бібліотек мов 
програмування (http.server)
Такі програми складаються з двох блоків
- запуск/зупинка сервера, підготовка інжекцій, конфігурацій тощо
- обслуговування запитів
Оброблення запитів покладається на клас-нащадок BaseHTTPRequestHandler
Запуск сервера -- на об'єкт HTTPServer
Особливості (відмінності від CGI)
- сервер запускається засобами мови (Python), шлях до інтерпретатора
зазначати не треба, сторонніх серверів також не вимагається.
- серверу потрібен вільний порт для запуску
- метод-обробник не отримує параметрів (типу Request/Response), всі 
дані передаються як поля/властивості класу (BaseHTTPRequestHandler)
- тіло відповіді формується записом у поле wfile у бінарному виді
(рядок треба перевести у байти)
print() виводить у консоль запуску, у відповідь ці дані не потрапляють
- методи обробника відповідають за методи запиту (do_GET, do_POST, ...)
і всі запити потрапляють у них (у тому числі запити на файли, навіть якщо
файл існує). Задача 

Cookie це --
а) загловки пакету HTTP-пакету, які за вимогами стандартів повинні включатися
у всі запити клієнтом.
б) елементи (частіше за все - файли), які зберігає та обслуговує клієнт (браузер)
з метою виключення їх до запитів та забезпечання достатнього часу їх існування
(у тому числі після перезапуску клієнта)

Cookie встановлюються заголовком Set-Cookie: name=value; expires=[DateTime]; path=/
Якщо треба декілька Cookie, вони передаються окремими заголовками Set-Cookie

Отримавши запит з таким заголовком клієнт (браузер) повинен зберігати ці дані в 
усі запити (за шаблоном path) протягом терміну (expires) включати заголовок
Cookie: name=value; name2=value2; name3=value3
у якому всі Cookie включаються через "; ", тільки імена та значення (інше ігнорується)

Видалення Cookie - повторне її встановлення з "минулим" терміном
Встановити також можна засобами JS через document.cookie 

Client                          Server 
GET / 
Host: localhost ------------>   HTTP 1/1 200 OK
                                Set-Cookie: name1=value1; path=/
                                Set-Cookie: name2=value2; expires=[DateTime];
    <------------------------   Set-Cookie: name3=value3;
GET /
Host: localhost
Cookie: name1=value1; name2=value2; name3=value3 -->                           